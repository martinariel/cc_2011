<?php


/**
 * Base class that represents a row from the 'persona' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.5.6 on:
 *
 * Fri Mar  4 00:48:22 2011
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePersona extends BaseObject  implements Persistent
{

	/**
	 * Peer class name
	 */
	const PEER = 'PersonaPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        PersonaPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the nombre field.
	 * Note: this column has a database default value of: ''
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the dni field.
	 * Note: this column has a database default value of: '0'
	 * @var        string
	 */
	protected $dni;

	/**
	 * The value for the fecha_nacimiento field.
	 * Note: this column has a database default value of: NULL
	 * @var        string
	 */
	protected $fecha_nacimiento;

	/**
	 * The value for the sexo field.
	 * Note: this column has a database default value of: 'X'
	 * @var        string
	 */
	protected $sexo;

	/**
	 * The value for the peso field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $peso;

	/**
	 * The value for the altura field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $altura;

	/**
	 * The value for the email field.
	 * @var        string
	 */
	protected $email;

	/**
	 * The value for the telefono field.
	 * @var        string
	 */
	protected $telefono;

	/**
	 * The value for the celular field.
	 * @var        string
	 */
	protected $celular;

	/**
	 * The value for the calzado field.
	 * Note: this column has a database default value of: '-1'
	 * @var        string
	 */
	protected $calzado;

	/**
	 * The value for the pantalon field.
	 * Note: this column has a database default value of: '-1'
	 * @var        string
	 */
	protected $pantalon;

	/**
	 * The value for the camisa field.
	 * Note: this column has a database default value of: '-1'
	 * @var        string
	 */
	protected $camisa;

	/**
	 * The value for the observaciones field.
	 * @var        string
	 */
	protected $observaciones;

	/**
	 * The value for the fecha_actualizacion field.
	 * Note: this column has a database default value of: NULL
	 * @var        string
	 */
	protected $fecha_actualizacion;

	/**
	 * The value for the nacionalidad_id field.
	 * @var        int
	 */
	protected $nacionalidad_id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * @var        Nacionalidad
	 */
	protected $aNacionalidad;

	/**
	 * @var        array Media[] Collection to store aggregation of Media objects.
	 */
	protected $collMedias;

	/**
	 * @var        array PersonaCasting[] Collection to store aggregation of PersonaCasting objects.
	 */
	protected $collPersonaCastings;

	/**
	 * @var        array PersonaScouting[] Collection to store aggregation of PersonaScouting objects.
	 */
	protected $collPersonaScoutings;

	/**
	 * @var        array PersonaLenguaje[] Collection to store aggregation of PersonaLenguaje objects.
	 */
	protected $collPersonaLenguajes;

	/**
	 * @var        array PersonaLabel[] Collection to store aggregation of PersonaLabel objects.
	 */
	protected $collPersonaLabels;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->nombre = '';
		$this->dni = '0';
		$this->fecha_nacimiento = NULL;
		$this->sexo = 'X';
		$this->peso = 0;
		$this->altura = 0;
		$this->calzado = '-1';
		$this->pantalon = '-1';
		$this->camisa = '-1';
		$this->fecha_actualizacion = NULL;
	}

	/**
	 * Initializes internal state of BasePersona object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [nombre] column value.
	 * 
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [dni] column value.
	 * 
	 * @return     string
	 */
	public function getDni()
	{
		return $this->dni;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_nacimiento] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaNacimiento($format = 'Y-m-d')
	{
		if ($this->fecha_nacimiento === null) {
			return null;
		}


		if ($this->fecha_nacimiento === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_nacimiento);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_nacimiento, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [sexo] column value.
	 * 
	 * @return     string
	 */
	public function getSexo()
	{
		return $this->sexo;
	}

	/**
	 * Get the [peso] column value.
	 * 
	 * @return     int
	 */
	public function getPeso()
	{
		return $this->peso;
	}

	/**
	 * Get the [altura] column value.
	 * 
	 * @return     int
	 */
	public function getAltura()
	{
		return $this->altura;
	}

	/**
	 * Get the [email] column value.
	 * 
	 * @return     string
	 */
	public function getEmail()
	{
		return $this->email;
	}

	/**
	 * Get the [telefono] column value.
	 * 
	 * @return     string
	 */
	public function getTelefono()
	{
		return $this->telefono;
	}

	/**
	 * Get the [celular] column value.
	 * 
	 * @return     string
	 */
	public function getCelular()
	{
		return $this->celular;
	}

	/**
	 * Get the [calzado] column value.
	 * 
	 * @return     string
	 */
	public function getCalzado()
	{
		return $this->calzado;
	}

	/**
	 * Get the [pantalon] column value.
	 * 
	 * @return     string
	 */
	public function getPantalon()
	{
		return $this->pantalon;
	}

	/**
	 * Get the [camisa] column value.
	 * 
	 * @return     string
	 */
	public function getCamisa()
	{
		return $this->camisa;
	}

	/**
	 * Get the [observaciones] column value.
	 * 
	 * @return     string
	 */
	public function getObservaciones()
	{
		return $this->observaciones;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_actualizacion] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaActualizacion($format = 'Y-m-d')
	{
		if ($this->fecha_actualizacion === null) {
			return null;
		}


		if ($this->fecha_actualizacion === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_actualizacion);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_actualizacion, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [nacionalidad_id] column value.
	 * 
	 * @return     int
	 */
	public function getNacionalidadId()
	{
		return $this->nacionalidad_id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = PersonaPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v || $this->isNew()) {
			$this->nombre = $v;
			$this->modifiedColumns[] = PersonaPeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [dni] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setDni($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->dni !== $v || $this->isNew()) {
			$this->dni = $v;
			$this->modifiedColumns[] = PersonaPeer::DNI;
		}

		return $this;
	} // setDni()

	/**
	 * Sets the value of [fecha_nacimiento] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setFechaNacimiento($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_nacimiento !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_nacimiento !== null && $tmpDt = new DateTime($this->fecha_nacimiento)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					|| ($dt->format('Y-m-d') === NULL) // or the entered value matches the default
					)
			{
				$this->fecha_nacimiento = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = PersonaPeer::FECHA_NACIMIENTO;
			}
		} // if either are not null

		return $this;
	} // setFechaNacimiento()

	/**
	 * Set the value of [sexo] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setSexo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->sexo !== $v || $this->isNew()) {
			$this->sexo = $v;
			$this->modifiedColumns[] = PersonaPeer::SEXO;
		}

		return $this;
	} // setSexo()

	/**
	 * Set the value of [peso] column.
	 * 
	 * @param      int $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setPeso($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->peso !== $v || $this->isNew()) {
			$this->peso = $v;
			$this->modifiedColumns[] = PersonaPeer::PESO;
		}

		return $this;
	} // setPeso()

	/**
	 * Set the value of [altura] column.
	 * 
	 * @param      int $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setAltura($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->altura !== $v || $this->isNew()) {
			$this->altura = $v;
			$this->modifiedColumns[] = PersonaPeer::ALTURA;
		}

		return $this;
	} // setAltura()

	/**
	 * Set the value of [email] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setEmail($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->email !== $v) {
			$this->email = $v;
			$this->modifiedColumns[] = PersonaPeer::EMAIL;
		}

		return $this;
	} // setEmail()

	/**
	 * Set the value of [telefono] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setTelefono($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->telefono !== $v) {
			$this->telefono = $v;
			$this->modifiedColumns[] = PersonaPeer::TELEFONO;
		}

		return $this;
	} // setTelefono()

	/**
	 * Set the value of [celular] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setCelular($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->celular !== $v) {
			$this->celular = $v;
			$this->modifiedColumns[] = PersonaPeer::CELULAR;
		}

		return $this;
	} // setCelular()

	/**
	 * Set the value of [calzado] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setCalzado($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->calzado !== $v || $this->isNew()) {
			$this->calzado = $v;
			$this->modifiedColumns[] = PersonaPeer::CALZADO;
		}

		return $this;
	} // setCalzado()

	/**
	 * Set the value of [pantalon] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setPantalon($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->pantalon !== $v || $this->isNew()) {
			$this->pantalon = $v;
			$this->modifiedColumns[] = PersonaPeer::PANTALON;
		}

		return $this;
	} // setPantalon()

	/**
	 * Set the value of [camisa] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setCamisa($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->camisa !== $v || $this->isNew()) {
			$this->camisa = $v;
			$this->modifiedColumns[] = PersonaPeer::CAMISA;
		}

		return $this;
	} // setCamisa()

	/**
	 * Set the value of [observaciones] column.
	 * 
	 * @param      string $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setObservaciones($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->observaciones !== $v) {
			$this->observaciones = $v;
			$this->modifiedColumns[] = PersonaPeer::OBSERVACIONES;
		}

		return $this;
	} // setObservaciones()

	/**
	 * Sets the value of [fecha_actualizacion] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setFechaActualizacion($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_actualizacion !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_actualizacion !== null && $tmpDt = new DateTime($this->fecha_actualizacion)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					|| ($dt->format('Y-m-d') === NULL) // or the entered value matches the default
					)
			{
				$this->fecha_actualizacion = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = PersonaPeer::FECHA_ACTUALIZACION;
			}
		} // if either are not null

		return $this;
	} // setFechaActualizacion()

	/**
	 * Set the value of [nacionalidad_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setNacionalidadId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->nacionalidad_id !== $v) {
			$this->nacionalidad_id = $v;
			$this->modifiedColumns[] = PersonaPeer::NACIONALIDAD_ID;
		}

		if ($this->aNacionalidad !== null && $this->aNacionalidad->getId() !== $v) {
			$this->aNacionalidad = null;
		}

		return $this;
	} // setNacionalidadId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Persona The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = PersonaPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->nombre !== '') {
				return false;
			}

			if ($this->dni !== '0') {
				return false;
			}

			if ($this->fecha_nacimiento !== NULL) {
				return false;
			}

			if ($this->sexo !== 'X') {
				return false;
			}

			if ($this->peso !== 0) {
				return false;
			}

			if ($this->altura !== 0) {
				return false;
			}

			if ($this->calzado !== '-1') {
				return false;
			}

			if ($this->pantalon !== '-1') {
				return false;
			}

			if ($this->camisa !== '-1') {
				return false;
			}

			if ($this->fecha_actualizacion !== NULL) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->nombre = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->dni = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->fecha_nacimiento = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->sexo = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->peso = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->altura = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->email = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->telefono = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->celular = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->calzado = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->pantalon = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->camisa = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->observaciones = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->fecha_actualizacion = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->nacionalidad_id = ($row[$startcol + 15] !== null) ? (int) $row[$startcol + 15] : null;
			$this->created_at = ($row[$startcol + 16] !== null) ? (string) $row[$startcol + 16] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			return $startcol + 17; // 17 = PersonaPeer::NUM_COLUMNS - PersonaPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Persona object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aNacionalidad !== null && $this->nacionalidad_id !== $this->aNacionalidad->getId()) {
			$this->aNacionalidad = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PersonaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = PersonaPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aNacionalidad = null;
			$this->collMedias = null;

			$this->collPersonaCastings = null;

			$this->collPersonaScoutings = null;

			$this->collPersonaLenguajes = null;

			$this->collPersonaLabels = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PersonaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BasePersona:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			    return;
			  }
			}

			if ($ret) {
				PersonaQuery::create()
					->filterByPrimaryKey($this->getPrimaryKey())
					->delete($con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BasePersona:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$con->commit();
				$this->setDeleted(true);
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PersonaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BasePersona:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			  	$con->commit();
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			
			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(PersonaPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BasePersona:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				PersonaPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aNacionalidad !== null) {
				if ($this->aNacionalidad->isModified() || $this->aNacionalidad->isNew()) {
					$affectedRows += $this->aNacionalidad->save($con);
				}
				$this->setNacionalidad($this->aNacionalidad);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = PersonaPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$criteria = $this->buildCriteria();
					if ($criteria->keyContainsValue(PersonaPeer::ID) ) {
						throw new PropelException('Cannot insert a value for auto-increment primary key ('.PersonaPeer::ID.')');
					}

					$pk = BasePeer::doInsert($criteria, $con);
					$affectedRows += 1;
					$this->setId($pk);  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					$affectedRows += PersonaPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collMedias !== null) {
				foreach ($this->collMedias as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPersonaCastings !== null) {
				foreach ($this->collPersonaCastings as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPersonaScoutings !== null) {
				foreach ($this->collPersonaScoutings as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPersonaLenguajes !== null) {
				foreach ($this->collPersonaLenguajes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPersonaLabels !== null) {
				foreach ($this->collPersonaLabels as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aNacionalidad !== null) {
				if (!$this->aNacionalidad->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aNacionalidad->getValidationFailures());
				}
			}


			if (($retval = PersonaPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collMedias !== null) {
					foreach ($this->collMedias as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPersonaCastings !== null) {
					foreach ($this->collPersonaCastings as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPersonaScoutings !== null) {
					foreach ($this->collPersonaScoutings as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPersonaLenguajes !== null) {
					foreach ($this->collPersonaLenguajes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPersonaLabels !== null) {
					foreach ($this->collPersonaLabels as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = PersonaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getNombre();
				break;
			case 2:
				return $this->getDni();
				break;
			case 3:
				return $this->getFechaNacimiento();
				break;
			case 4:
				return $this->getSexo();
				break;
			case 5:
				return $this->getPeso();
				break;
			case 6:
				return $this->getAltura();
				break;
			case 7:
				return $this->getEmail();
				break;
			case 8:
				return $this->getTelefono();
				break;
			case 9:
				return $this->getCelular();
				break;
			case 10:
				return $this->getCalzado();
				break;
			case 11:
				return $this->getPantalon();
				break;
			case 12:
				return $this->getCamisa();
				break;
			case 13:
				return $this->getObservaciones();
				break;
			case 14:
				return $this->getFechaActualizacion();
				break;
			case 15:
				return $this->getNacionalidadId();
				break;
			case 16:
				return $this->getCreatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 *                    Defaults to BasePeer::TYPE_PHPNAME.
	 * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to TRUE.
	 * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
	 *
	 * @return    array an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $includeForeignObjects = false)
	{
		$keys = PersonaPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getNombre(),
			$keys[2] => $this->getDni(),
			$keys[3] => $this->getFechaNacimiento(),
			$keys[4] => $this->getSexo(),
			$keys[5] => $this->getPeso(),
			$keys[6] => $this->getAltura(),
			$keys[7] => $this->getEmail(),
			$keys[8] => $this->getTelefono(),
			$keys[9] => $this->getCelular(),
			$keys[10] => $this->getCalzado(),
			$keys[11] => $this->getPantalon(),
			$keys[12] => $this->getCamisa(),
			$keys[13] => $this->getObservaciones(),
			$keys[14] => $this->getFechaActualizacion(),
			$keys[15] => $this->getNacionalidadId(),
			$keys[16] => $this->getCreatedAt(),
		);
		if ($includeForeignObjects) {
			if (null !== $this->aNacionalidad) {
				$result['Nacionalidad'] = $this->aNacionalidad->toArray($keyType, $includeLazyLoadColumns, true);
			}
		}
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = PersonaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setNombre($value);
				break;
			case 2:
				$this->setDni($value);
				break;
			case 3:
				$this->setFechaNacimiento($value);
				break;
			case 4:
				$this->setSexo($value);
				break;
			case 5:
				$this->setPeso($value);
				break;
			case 6:
				$this->setAltura($value);
				break;
			case 7:
				$this->setEmail($value);
				break;
			case 8:
				$this->setTelefono($value);
				break;
			case 9:
				$this->setCelular($value);
				break;
			case 10:
				$this->setCalzado($value);
				break;
			case 11:
				$this->setPantalon($value);
				break;
			case 12:
				$this->setCamisa($value);
				break;
			case 13:
				$this->setObservaciones($value);
				break;
			case 14:
				$this->setFechaActualizacion($value);
				break;
			case 15:
				$this->setNacionalidadId($value);
				break;
			case 16:
				$this->setCreatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = PersonaPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNombre($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDni($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setFechaNacimiento($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setSexo($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setPeso($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setAltura($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setEmail($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setTelefono($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCelular($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setCalzado($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setPantalon($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setCamisa($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setObservaciones($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setFechaActualizacion($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setNacionalidadId($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setCreatedAt($arr[$keys[16]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(PersonaPeer::DATABASE_NAME);

		if ($this->isColumnModified(PersonaPeer::ID)) $criteria->add(PersonaPeer::ID, $this->id);
		if ($this->isColumnModified(PersonaPeer::NOMBRE)) $criteria->add(PersonaPeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(PersonaPeer::DNI)) $criteria->add(PersonaPeer::DNI, $this->dni);
		if ($this->isColumnModified(PersonaPeer::FECHA_NACIMIENTO)) $criteria->add(PersonaPeer::FECHA_NACIMIENTO, $this->fecha_nacimiento);
		if ($this->isColumnModified(PersonaPeer::SEXO)) $criteria->add(PersonaPeer::SEXO, $this->sexo);
		if ($this->isColumnModified(PersonaPeer::PESO)) $criteria->add(PersonaPeer::PESO, $this->peso);
		if ($this->isColumnModified(PersonaPeer::ALTURA)) $criteria->add(PersonaPeer::ALTURA, $this->altura);
		if ($this->isColumnModified(PersonaPeer::EMAIL)) $criteria->add(PersonaPeer::EMAIL, $this->email);
		if ($this->isColumnModified(PersonaPeer::TELEFONO)) $criteria->add(PersonaPeer::TELEFONO, $this->telefono);
		if ($this->isColumnModified(PersonaPeer::CELULAR)) $criteria->add(PersonaPeer::CELULAR, $this->celular);
		if ($this->isColumnModified(PersonaPeer::CALZADO)) $criteria->add(PersonaPeer::CALZADO, $this->calzado);
		if ($this->isColumnModified(PersonaPeer::PANTALON)) $criteria->add(PersonaPeer::PANTALON, $this->pantalon);
		if ($this->isColumnModified(PersonaPeer::CAMISA)) $criteria->add(PersonaPeer::CAMISA, $this->camisa);
		if ($this->isColumnModified(PersonaPeer::OBSERVACIONES)) $criteria->add(PersonaPeer::OBSERVACIONES, $this->observaciones);
		if ($this->isColumnModified(PersonaPeer::FECHA_ACTUALIZACION)) $criteria->add(PersonaPeer::FECHA_ACTUALIZACION, $this->fecha_actualizacion);
		if ($this->isColumnModified(PersonaPeer::NACIONALIDAD_ID)) $criteria->add(PersonaPeer::NACIONALIDAD_ID, $this->nacionalidad_id);
		if ($this->isColumnModified(PersonaPeer::CREATED_AT)) $criteria->add(PersonaPeer::CREATED_AT, $this->created_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(PersonaPeer::DATABASE_NAME);
		$criteria->add(PersonaPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Returns true if the primary key for this object is null.
	 * @return     boolean
	 */
	public function isPrimaryKeyNull()
	{
		return null === $this->getId();
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Persona (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{
		$copyObj->setNombre($this->nombre);
		$copyObj->setDni($this->dni);
		$copyObj->setFechaNacimiento($this->fecha_nacimiento);
		$copyObj->setSexo($this->sexo);
		$copyObj->setPeso($this->peso);
		$copyObj->setAltura($this->altura);
		$copyObj->setEmail($this->email);
		$copyObj->setTelefono($this->telefono);
		$copyObj->setCelular($this->celular);
		$copyObj->setCalzado($this->calzado);
		$copyObj->setPantalon($this->pantalon);
		$copyObj->setCamisa($this->camisa);
		$copyObj->setObservaciones($this->observaciones);
		$copyObj->setFechaActualizacion($this->fecha_actualizacion);
		$copyObj->setNacionalidadId($this->nacionalidad_id);
		$copyObj->setCreatedAt($this->created_at);

		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getMedias() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMedia($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPersonaCastings() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPersonaCasting($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPersonaScoutings() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPersonaScouting($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPersonaLenguajes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPersonaLenguaje($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPersonaLabels() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPersonaLabel($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);
		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value
	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Persona Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     PersonaPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new PersonaPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Nacionalidad object.
	 *
	 * @param      Nacionalidad $v
	 * @return     Persona The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setNacionalidad(Nacionalidad $v = null)
	{
		if ($v === null) {
			$this->setNacionalidadId(NULL);
		} else {
			$this->setNacionalidadId($v->getId());
		}

		$this->aNacionalidad = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Nacionalidad object, it will not be re-added.
		if ($v !== null) {
			$v->addPersona($this);
		}

		return $this;
	}


	/**
	 * Get the associated Nacionalidad object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Nacionalidad The associated Nacionalidad object.
	 * @throws     PropelException
	 */
	public function getNacionalidad(PropelPDO $con = null)
	{
		if ($this->aNacionalidad === null && ($this->nacionalidad_id !== null)) {
			$this->aNacionalidad = NacionalidadQuery::create()->findPk($this->nacionalidad_id, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aNacionalidad->addPersonas($this);
			 */
		}
		return $this->aNacionalidad;
	}

	/**
	 * Clears out the collMedias collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMedias()
	 */
	public function clearMedias()
	{
		$this->collMedias = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMedias collection.
	 *
	 * By default this just sets the collMedias collection to an empty array (like clearcollMedias());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMedias()
	{
		$this->collMedias = new PropelObjectCollection();
		$this->collMedias->setModel('Media');
	}

	/**
	 * Gets an array of Media objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Persona is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array Media[] List of Media objects
	 * @throws     PropelException
	 */
	public function getMedias($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collMedias || null !== $criteria) {
			if ($this->isNew() && null === $this->collMedias) {
				// return empty collection
				$this->initMedias();
			} else {
				$collMedias = MediaQuery::create(null, $criteria)
					->filterByPersona($this)
					->find($con);
				if (null !== $criteria) {
					return $collMedias;
				}
				$this->collMedias = $collMedias;
			}
		}
		return $this->collMedias;
	}

	/**
	 * Returns the number of related Media objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Media objects.
	 * @throws     PropelException
	 */
	public function countMedias(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collMedias || null !== $criteria) {
			if ($this->isNew() && null === $this->collMedias) {
				return 0;
			} else {
				$query = MediaQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByPersona($this)
					->count($con);
			}
		} else {
			return count($this->collMedias);
		}
	}

	/**
	 * Method called to associate a Media object to this object
	 * through the Media foreign key attribute.
	 *
	 * @param      Media $l Media
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMedia(Media $l)
	{
		if ($this->collMedias === null) {
			$this->initMedias();
		}
		if (!$this->collMedias->contains($l)) { // only add it if the **same** object is not already associated
			$this->collMedias[]= $l;
			$l->setPersona($this);
		}
	}

	/**
	 * Clears out the collPersonaCastings collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPersonaCastings()
	 */
	public function clearPersonaCastings()
	{
		$this->collPersonaCastings = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPersonaCastings collection.
	 *
	 * By default this just sets the collPersonaCastings collection to an empty array (like clearcollPersonaCastings());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPersonaCastings()
	{
		$this->collPersonaCastings = new PropelObjectCollection();
		$this->collPersonaCastings->setModel('PersonaCasting');
	}

	/**
	 * Gets an array of PersonaCasting objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Persona is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array PersonaCasting[] List of PersonaCasting objects
	 * @throws     PropelException
	 */
	public function getPersonaCastings($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collPersonaCastings || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaCastings) {
				// return empty collection
				$this->initPersonaCastings();
			} else {
				$collPersonaCastings = PersonaCastingQuery::create(null, $criteria)
					->filterByPersona($this)
					->find($con);
				if (null !== $criteria) {
					return $collPersonaCastings;
				}
				$this->collPersonaCastings = $collPersonaCastings;
			}
		}
		return $this->collPersonaCastings;
	}

	/**
	 * Returns the number of related PersonaCasting objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PersonaCasting objects.
	 * @throws     PropelException
	 */
	public function countPersonaCastings(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collPersonaCastings || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaCastings) {
				return 0;
			} else {
				$query = PersonaCastingQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByPersona($this)
					->count($con);
			}
		} else {
			return count($this->collPersonaCastings);
		}
	}

	/**
	 * Method called to associate a PersonaCasting object to this object
	 * through the PersonaCasting foreign key attribute.
	 *
	 * @param      PersonaCasting $l PersonaCasting
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPersonaCasting(PersonaCasting $l)
	{
		if ($this->collPersonaCastings === null) {
			$this->initPersonaCastings();
		}
		if (!$this->collPersonaCastings->contains($l)) { // only add it if the **same** object is not already associated
			$this->collPersonaCastings[]= $l;
			$l->setPersona($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaCastings from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaCasting[] List of PersonaCasting objects
	 */
	public function getPersonaCastingsJoinCasting($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaCastingQuery::create(null, $criteria);
		$query->joinWith('Casting', $join_behavior);

		return $this->getPersonaCastings($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaCastings from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaCasting[] List of PersonaCasting objects
	 */
	public function getPersonaCastingsJoinAgencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaCastingQuery::create(null, $criteria);
		$query->joinWith('Agencia', $join_behavior);

		return $this->getPersonaCastings($query, $con);
	}

	/**
	 * Clears out the collPersonaScoutings collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPersonaScoutings()
	 */
	public function clearPersonaScoutings()
	{
		$this->collPersonaScoutings = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPersonaScoutings collection.
	 *
	 * By default this just sets the collPersonaScoutings collection to an empty array (like clearcollPersonaScoutings());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPersonaScoutings()
	{
		$this->collPersonaScoutings = new PropelObjectCollection();
		$this->collPersonaScoutings->setModel('PersonaScouting');
	}

	/**
	 * Gets an array of PersonaScouting objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Persona is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array PersonaScouting[] List of PersonaScouting objects
	 * @throws     PropelException
	 */
	public function getPersonaScoutings($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collPersonaScoutings || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaScoutings) {
				// return empty collection
				$this->initPersonaScoutings();
			} else {
				$collPersonaScoutings = PersonaScoutingQuery::create(null, $criteria)
					->filterByPersona($this)
					->find($con);
				if (null !== $criteria) {
					return $collPersonaScoutings;
				}
				$this->collPersonaScoutings = $collPersonaScoutings;
			}
		}
		return $this->collPersonaScoutings;
	}

	/**
	 * Returns the number of related PersonaScouting objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PersonaScouting objects.
	 * @throws     PropelException
	 */
	public function countPersonaScoutings(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collPersonaScoutings || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaScoutings) {
				return 0;
			} else {
				$query = PersonaScoutingQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByPersona($this)
					->count($con);
			}
		} else {
			return count($this->collPersonaScoutings);
		}
	}

	/**
	 * Method called to associate a PersonaScouting object to this object
	 * through the PersonaScouting foreign key attribute.
	 *
	 * @param      PersonaScouting $l PersonaScouting
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPersonaScouting(PersonaScouting $l)
	{
		if ($this->collPersonaScoutings === null) {
			$this->initPersonaScoutings();
		}
		if (!$this->collPersonaScoutings->contains($l)) { // only add it if the **same** object is not already associated
			$this->collPersonaScoutings[]= $l;
			$l->setPersona($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaScoutings from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaScouting[] List of PersonaScouting objects
	 */
	public function getPersonaScoutingsJoinLugar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaScoutingQuery::create(null, $criteria);
		$query->joinWith('Lugar', $join_behavior);

		return $this->getPersonaScoutings($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaScoutings from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaScouting[] List of PersonaScouting objects
	 */
	public function getPersonaScoutingsJoinNacionalidad($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaScoutingQuery::create(null, $criteria);
		$query->joinWith('Nacionalidad', $join_behavior);

		return $this->getPersonaScoutings($query, $con);
	}

	/**
	 * Clears out the collPersonaLenguajes collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPersonaLenguajes()
	 */
	public function clearPersonaLenguajes()
	{
		$this->collPersonaLenguajes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPersonaLenguajes collection.
	 *
	 * By default this just sets the collPersonaLenguajes collection to an empty array (like clearcollPersonaLenguajes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPersonaLenguajes()
	{
		$this->collPersonaLenguajes = new PropelObjectCollection();
		$this->collPersonaLenguajes->setModel('PersonaLenguaje');
	}

	/**
	 * Gets an array of PersonaLenguaje objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Persona is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array PersonaLenguaje[] List of PersonaLenguaje objects
	 * @throws     PropelException
	 */
	public function getPersonaLenguajes($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collPersonaLenguajes || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaLenguajes) {
				// return empty collection
				$this->initPersonaLenguajes();
			} else {
				$collPersonaLenguajes = PersonaLenguajeQuery::create(null, $criteria)
					->filterByPersona($this)
					->find($con);
				if (null !== $criteria) {
					return $collPersonaLenguajes;
				}
				$this->collPersonaLenguajes = $collPersonaLenguajes;
			}
		}
		return $this->collPersonaLenguajes;
	}

	/**
	 * Returns the number of related PersonaLenguaje objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PersonaLenguaje objects.
	 * @throws     PropelException
	 */
	public function countPersonaLenguajes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collPersonaLenguajes || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaLenguajes) {
				return 0;
			} else {
				$query = PersonaLenguajeQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByPersona($this)
					->count($con);
			}
		} else {
			return count($this->collPersonaLenguajes);
		}
	}

	/**
	 * Method called to associate a PersonaLenguaje object to this object
	 * through the PersonaLenguaje foreign key attribute.
	 *
	 * @param      PersonaLenguaje $l PersonaLenguaje
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPersonaLenguaje(PersonaLenguaje $l)
	{
		if ($this->collPersonaLenguajes === null) {
			$this->initPersonaLenguajes();
		}
		if (!$this->collPersonaLenguajes->contains($l)) { // only add it if the **same** object is not already associated
			$this->collPersonaLenguajes[]= $l;
			$l->setPersona($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaLenguajes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaLenguaje[] List of PersonaLenguaje objects
	 */
	public function getPersonaLenguajesJoinLenguaje($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaLenguajeQuery::create(null, $criteria);
		$query->joinWith('Lenguaje', $join_behavior);

		return $this->getPersonaLenguajes($query, $con);
	}

	/**
	 * Clears out the collPersonaLabels collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPersonaLabels()
	 */
	public function clearPersonaLabels()
	{
		$this->collPersonaLabels = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPersonaLabels collection.
	 *
	 * By default this just sets the collPersonaLabels collection to an empty array (like clearcollPersonaLabels());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPersonaLabels()
	{
		$this->collPersonaLabels = new PropelObjectCollection();
		$this->collPersonaLabels->setModel('PersonaLabel');
	}

	/**
	 * Gets an array of PersonaLabel objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Persona is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array PersonaLabel[] List of PersonaLabel objects
	 * @throws     PropelException
	 */
	public function getPersonaLabels($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collPersonaLabels || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaLabels) {
				// return empty collection
				$this->initPersonaLabels();
			} else {
				$collPersonaLabels = PersonaLabelQuery::create(null, $criteria)
					->filterByPersona($this)
					->find($con);
				if (null !== $criteria) {
					return $collPersonaLabels;
				}
				$this->collPersonaLabels = $collPersonaLabels;
			}
		}
		return $this->collPersonaLabels;
	}

	/**
	 * Returns the number of related PersonaLabel objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PersonaLabel objects.
	 * @throws     PropelException
	 */
	public function countPersonaLabels(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collPersonaLabels || null !== $criteria) {
			if ($this->isNew() && null === $this->collPersonaLabels) {
				return 0;
			} else {
				$query = PersonaLabelQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByPersona($this)
					->count($con);
			}
		} else {
			return count($this->collPersonaLabels);
		}
	}

	/**
	 * Method called to associate a PersonaLabel object to this object
	 * through the PersonaLabel foreign key attribute.
	 *
	 * @param      PersonaLabel $l PersonaLabel
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPersonaLabel(PersonaLabel $l)
	{
		if ($this->collPersonaLabels === null) {
			$this->initPersonaLabels();
		}
		if (!$this->collPersonaLabels->contains($l)) { // only add it if the **same** object is not already associated
			$this->collPersonaLabels[]= $l;
			$l->setPersona($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Persona is new, it will return
	 * an empty collection; or if this Persona has previously
	 * been saved, it will retrieve related PersonaLabels from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Persona.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array PersonaLabel[] List of PersonaLabel objects
	 */
	public function getPersonaLabelsJoinLabel($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = PersonaLabelQuery::create(null, $criteria);
		$query->joinWith('Label', $join_behavior);

		return $this->getPersonaLabels($query, $con);
	}

	/**
	 * Clears the current object and sets all attributes to their default values
	 */
	public function clear()
	{
		$this->id = null;
		$this->nombre = null;
		$this->dni = null;
		$this->fecha_nacimiento = null;
		$this->sexo = null;
		$this->peso = null;
		$this->altura = null;
		$this->email = null;
		$this->telefono = null;
		$this->celular = null;
		$this->calzado = null;
		$this->pantalon = null;
		$this->camisa = null;
		$this->observaciones = null;
		$this->fecha_actualizacion = null;
		$this->nacionalidad_id = null;
		$this->created_at = null;
		$this->alreadyInSave = false;
		$this->alreadyInValidation = false;
		$this->clearAllReferences();
		$this->applyDefaultValues();
		$this->resetModified();
		$this->setNew(true);
		$this->setDeleted(false);
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collMedias) {
				foreach ((array) $this->collMedias as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPersonaCastings) {
				foreach ((array) $this->collPersonaCastings as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPersonaScoutings) {
				foreach ((array) $this->collPersonaScoutings as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPersonaLenguajes) {
				foreach ((array) $this->collPersonaLenguajes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPersonaLabels) {
				foreach ((array) $this->collPersonaLabels as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collMedias = null;
		$this->collPersonaCastings = null;
		$this->collPersonaScoutings = null;
		$this->collPersonaLenguajes = null;
		$this->collPersonaLabels = null;
		$this->aNacionalidad = null;
	}

	/**
	 * Catches calls to virtual methods
	 */
	public function __call($name, $params)
	{
		// symfony_behaviors behavior
		if ($callable = sfMixer::getCallable('BasePersona:' . $name))
		{
		  array_unshift($params, $this);
		  return call_user_func_array($callable, $params);
		}

		if (preg_match('/get(\w+)/', $name, $matches)) {
			$virtualColumn = $matches[1];
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
			// no lcfirst in php<5.3...
			$virtualColumn[0] = strtolower($virtualColumn[0]);
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
		}
		return parent::__call($name, $params);
	}

} // BasePersona
